<aside role="note">
	<strong>Guest event bus</strong> (<code>emit</code> / <code>onEvent</code>) is documented as “coming soon” and is not in
	<code>microsandbox@0.3.12</code>. Use <strong>execution stream</strong> events (<code>ExecHandle.recv</code>) from the Runs
	page instead.
</aside>
